<h1>Saved Jobs</h1>

<ul>
    {{#each jobs}}
    <a href="{{link}}">{{title}}</a>
    <li>{{company}}</li>
    <li>{{summary}}</li>
    <button class="add-note" data-id="{{_id}}">Add Note</button>
    {{/each}}
</ul>