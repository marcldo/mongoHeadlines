<h4>Saved Jobs</h4>

<ul>
    {{#each jobs}}
    <a href="{{link}}">{{title}}</a>
    <li>{{company}}</li>
    <li>{{summary}}</li>
    <button class="add-note" data-id="{{_id}}">Add Note</button>
    <button class="delete" data-id="{{_id}}">Delete</button>
    {{/each}}
</ul>